#include<stdio.h>
#include<string.h>
int main()
{
int i,count char=0;
char str[50];
printf("enter th string");
scanf("%s",str);
for(i=0;a[0]!="\0";i++)
{
if(str[i]=' ')
{
count=char++;
}
}
printf("number of char is %d",char);
return 0;
}
