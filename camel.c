#include <stdio.h>
int casestr( char* str)
{ 
int i;
char ch;
ch=str[i];
for(i=0;i=ch;i++){
if(islower(ch))
{ ch=toupper(ch);
  printf("\n ch")
  } else{ printf("\n changed");}}return 0;
  }
  int main()
  { char* str;
   printf("\n enter the string");
   scanf("%s",&str);
   casestr(str);
   return 0;
}

