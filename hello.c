#include<stdio.h>
#include<string.h>
int main()
{
    char str[50];
    int length;
    int i;
    int count;
    printf("Enter the string\n");
    gets(str);
    length=strlen(str);
        if(str[i]==' ')
        {
            count++;
  printf("%d",count+1);
  }
  return 0;
}
