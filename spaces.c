#include<stdio.h>
int main()
{
int i,spaces;
char str[50];
printf("enter th string");
scanf("%s",a);
for(i=0;a[0]!="\0";i++)
{
if(str[i]=' ')
{
spaces++;
}
}
printf("number of spaces is %d",spaces);
}
